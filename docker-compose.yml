version: '3.9'

services:
  backend:
    build:
      context: ./backend
      dockerfile: Dockerfile
    container_name: finance_backend
    env_file:
      - .env
    environment:
      PORT: 8080
    ports:
      - "8080:8080"
